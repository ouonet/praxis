# discover → design

::: info 已合并到 design
`discover` 技能已合并到 [`design`](./design)。

目标模糊时，加载 `design`。它会逐轮提一个澄清问题，直到问题具体到可以写规范为止。工作笔记支持假设和实验记录，并有明确的放弃路径——如果探索结束时没有构建决策，直接在此停止。

知识型产出（协议规范、逆向工程发现、数据结构图）通过 `archive` 保存到 `docs/decisions/`。
:::
