# Review（评审）

代码合并前检查。

## 检查清单

- 所有测试通过。
- 代码符合规范。
- 规范反映实际行为（如果规范改变了）。
- 没有明显的 bug。
- 无性能回归。

## 如果不符

停在这里。要么 `tdd` 修复问题，要么回到 `design` 如果规范需要改变。

## 如果符合

确认，移到下一步：`archive`（合并到 living docs）然后 `ship`（merge / PR）。
