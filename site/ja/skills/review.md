# Review（レビュー）

コードをマージする前に確認する。

## チェックリスト

- すべてのテストが通過している。
- コードが仕様に準拠している。
- 仕様が実際の動作を反映している（仕様が変わった場合）。
- 明らかなバグがない。
- パフォーマンスのリグレッションがない。

## 一致しない場合

ここで止まる。`tdd` で問題を修正するか、仕様を変更する必要があれば `design` に戻る。

## 一致する場合

確認し、次のステップに進む：`archive`（living docsにマージ）、次に `ship`（マージ / PR）。
