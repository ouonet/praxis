# Subagents（サブエージェント）

計画に `[parallel]` マークのタスクがある場合、並行エージェントに派遣する。

## 派遣

各エージェントは1つまたは複数のタスクを受け取る。重複なし、各自の完了条件が明確。

## 実行

サブエージェントは独立して作業する。各タスクにacceptance条件がある。完了したらサブエージェントが報告する。

## 集約

調整エージェントが結果を収集し、タスクを完了としてマークし、コンフリクトのないマージを確認する。
