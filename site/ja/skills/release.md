# Release（リリース）

バージョン付け、タグ付け、公開。

## バージョンの確認

現在のバージョンは何か。新しいバージョンは何であるべきか（セマンティックバージョニングに従う）。

## CHANGELOGの更新

`Unreleased` をバージョン番号に改名する。リリース日を追加する。

## タグ

```bash
git tag v<version>
```

## Push & Publish

ユーザーに再確認する。

ユーザーの明示的な承認なしにpushしない。
