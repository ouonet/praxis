# Review（리뷰）

코드를 병합하기 전에 확인한다.

## 체크리스트

- 모든 테스트가 통과한다.
- 코드가 명세를 준수한다.
- 명세가 실제 동작을 반영한다 (명세가 바뀐 경우).
- 명백한 버그가 없다.
- 성능 회귀가 없다.

## 일치하지 않는 경우

여기서 멈춘다. `tdd`로 문제를 수정하거나, 명세를 변경해야 하면 `design`으로 돌아간다.

## 일치하는 경우

확인하고, 다음 단계로 진행: `archive` (living docs에 병합), 그런 다음 `ship` (병합 / PR).
