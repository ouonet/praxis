# Release（릴리스）

버전 지정, 태그 지정, 게시.

## 버전 확인

현재 버전은 무엇인가. 새 버전은 무엇이어야 하는가 (시맨틱 버저닝 준수).

## CHANGELOG 업데이트

`Unreleased`를 버전 번호로 이름을 바꾼다. 릴리스 날짜를 추가한다.

## 태그

```bash
git tag v<version>
```

## Push & Publish

사용자에게 재확인한다.

사용자의 명시적 승인 없이 push하지 않는다.
